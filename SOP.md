# JumpX Assets Portal - 极简操作指南 (SOP)

为保证我们宣发物料的 “唯一真实数据源 (Single Source of Truth)”，所有业务侧（官网、Demo、教案）的海报和配图都将统一通过本仓库进行管理。以下是针对运营和教研同学的极简操作规范。

## 🎯 核心工作流：如何上传或更新图片？

**无需写任何代码，只需 4 步即可完成上线！**

1. **进入目录**：打开 GitHub 仓库网页，依次点击进入 `courses/你的课程名/` 目录。
2. **点击上传**：点击右上角的 `Add file` -> 选择 `Upload files`。
3. **拖拽文件**：将处理好的图片（注意命名和体积规范）拖拽进虚线框内。
4. **提交生效**：在下方 `Commit changes` 处点击绿色按钮。

🎉 **大功告成！** 提交后请等待 10-15 秒钟。
刷新画廊网站 [assets.jumpx-labs.pages.dev](https://assets.jumpx-labs.pages.dev)，你就能在瀑布流中看到新图片，并可以点击 **"Copy CDN URL"** 复制带有防缓存时间戳的直接引用链接！

---

## ⚠️ 绝对不可触碰的红线（必须遵守）

为了保证网站和下游调用端的稳定，请所有成员严格遵守以下三条铁律：

### 🚫 1. 严禁修改文件名（覆盖更新原则）
如果你要替换一张现有的海报，请务必在本地将新图片**命名为与旧图片一模一样的名字**，然后上传。
- ✅ **正确**：用新的 `hero_poster.png` 覆盖旧的 `hero_poster.png`。
- ❌ **错误**：命名为 `hero_poster_v2.png`、`最新版_海报.png`。这样会导致外部引用了旧名字的网页直接挂掉（图裂）。

### 🚫 2. 严禁出现中文、空格或特殊符号
文件名只允许包含 **小写字母、数字和中划线 (`-`)**。
- ✅ **正确**：`ai-bootcamp-banner.jpg`
- ❌ **错误**：`AI 集训营 海报(1).jpg`

### 🚫 3. 严控图片体积
- **海报/照片** (`.jpg`, `.webp`)：单张**严禁超过 500KB**。请在本地压缩后再上传，否则会导致调用方网页加载极其缓慢。
- **图标/架构图**：强制使用无损且体积小的 `.svg` 格式。

---

## ❓ 常见问题排查 (FAQ)

**Q: 为什么我上传了图片，但在画廊里没看到？**
> A: 请检查文件后缀名是否为支持的格式（`.jpg`, `.png`, `.svg`, `.webp`）。系统会自动忽略不支持的格式。

**Q: 为什么我覆盖了新图，但是官网展示的还是旧图？**
> A: 这是浏览器或 CDN 的缓存机制导致的。虽然文件已经更新，但旧图仍在缓存中。请确保前端业务在调用你的 CDN 链接时，复制了画廊中提供的带有 `?v=xxx` 的时间戳链接，以强制刷新缓存。
